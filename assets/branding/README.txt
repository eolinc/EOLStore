Drop the official EOLStore logo here as:

    eolstore-logo.png

Any reasonable size works - it's displayed at a fixed 32px height in the
header and never stretched (object-fit: contain), so just keep it at its
original aspect ratio. Until this file exists, the header shows the
text/monogram fallback automatically (see index.html's onerror handler).
