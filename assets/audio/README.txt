Optional: drop a looping store theme track here as:

    store-theme.mp3

If this file exists, the header's music-note button loops it. If it's
missing, the button instead plays a short original synthesized loop
generated in-browser (js/services/audioService.js) - so the "background
music" feature works immediately without depending on any specific
licensed or trademarked track.
